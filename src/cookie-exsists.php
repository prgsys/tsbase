if(isset($_COOKIE['username'])) {
    // User cookie by username exists
    echo 'User cookie by username exists.';
} else {
    // User cookie by username does not exist
    echo 'User cookie by username does not exist.';
}
